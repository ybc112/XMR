# 多仓位独立记账方案设计（收益算法改造）

> 文档版本：v1.0 ｜ 日期：2026-09-20 ｜ 状态：**设计待评审，未开始编码**
> 关联合约：`StakingDApp.sol` ｜ 目标：修复"补投加速出局"与"复投清零"两个问题

---

## 一、背景与问题

### 问题 1：补投导致出局额度被「加速」消耗

当前合约中，补投会把本金**合并**计算：

```solidity
user.personalAmount += _amount;                     // 本金累加
user.exitLimit = user.personalAmount * EXIT_MULTIPLIER;  // 出局额度 = 本金 × 3
```

**实例**（100U 本金、赚到 280U 时补投 100U）：

| | 当前逻辑 |
|---|---|
| 补投后本金 | 200 U |
| 出局额度 | 600 U |
| 已赚 | 280 U（保留） |
| 剩余额度 | 320 U |
| 每天发放 | 2 U（200 × 1%） |
| **拿完剩余额度耗时** | **160 天** |

**问题本质**：原本按 100U 本金"慢慢赚"的剩余额度，被 200U 本金的速率提前抽干，**发放节奏被压缩一半**，对资金池形成额外压力。虽然总额仍是 3 倍（投 200 拿 600），但节奏不符合业务预期。

### 问题 2：出局后复投会清零未提取收益

```solidity
if (user.exited) {
    user.personalAmount = _amount;
    user.totalEarned = 0;
    user.pendingUSDT = 0;           // ⚠️ 未提取的 USDT 收益被清零
    user.pendingXMR = 0;            // ⚠️ 未提取的 XMR 收益被清零
    user.xmrWithdrawalPending = 0;  // ⚠️ 审核中的提现也被清零
}
```

XMR 提现门槛为 0.05，小额收益需累积多日才能提取；若出局即复投，用户会**直接损失**这些已赚收益。

---

## 二、改造目标（需求确认）

1. **每次投资生成一个独立仓位**，各自记账、各自 3 倍出局
2. **静态收益**：每个仓位按自身本金 × 1%/天 独立累计，各自封顶本金 3 倍
3. **动态收益**（推荐奖 / 团队奖）：**按仓位先后顺序填充** —— 先补满最早的仓位，补满后溢出到下一个仓位
4. **前端不体现「仓位」概念**，用户看到的是账号级汇总数据（无感改造）
5. 出局判定：单仓位拿满即停止产生收益；**所有仓位都拿满 → 账号出局**

### 业务实例（来自需求方）

| 时点 | 仓位 1 | 仓位 2 | 事件 |
|---|---|---|---|
| 初始 | 本金 100，额度 300，已赚 280（剩 20） | — | — |
| A 追加投资 100U | 本金 100，额度 300，已赚 280（剩 20） | 本金 100，额度 300，已赚 0（剩 300） | 生成仓位 2 |
| 直推 B 投资 1000U，A 获动态奖 100U | **+20**（补满 → 该仓位出局） | **+80** | 按仓位顺序填充 ✓ |

---

## 三、数据结构设计

### 3.1 仓位结构

```solidity
struct Position {
    uint256 principal;        // 该仓位本金（≥100 且为 100 的整数倍）
    uint256 earned;           // 该仓位累计已赚（静态 + 动态，统一计入）
    uint256 lastClaimPeriod;  // 该仓位最后结算周期（补投时间不同，各自独立计算）
    bool    closed;           // 是否已拿满 3 倍（停止产生收益）
}

mapping(address => Position[]) internal positions;   // 按投资时间顺序排列
```

### 3.2 账号级汇总（对外口径不变，前端无感）

| 字段 | 汇总口径 |
|---|---|
| `personalAmount` | Σ 所有仓位 `principal` |
| `exitLimit` | Σ 所有仓位 `principal × 3` |
| `totalEarned` | Σ 所有仓位 `earned` |
| `lastClaimDay` | 保留（兼容旧接口，取最新仓位的周期） |
| `exited` | 所有仓位均 `closed` 时为 true |

> 前端展示的"累计收益""出局进度"仍按汇总值计算，用户看不到仓位概念。

---

## 四、核心逻辑设计

### 4.1 投资（invest）

```
收到投资 _amount（校验：≥100、100 的整数倍、已注册）
  ├─ 新建仓位：principal = _amount
  │             earned = 0
  │             lastClaimPeriod = 当前周期
  │             closed = false
  ├─ 首次投资时，推荐人 activeDirectCount +1（沿用现有规则）
  ├─ 转账 USDT 入合约
  ├─ 更新等级 / 团队业绩
  └─ 触发推荐奖（动态收益，按下级规则发放给上级）
```

> 注：**不再是 `+=` 合并本金**，而是新增一个仓位。

### 4.2 静态收益（每日自动结算）

对**每个未关闭的仓位**独立计算：

```
对该仓位：
  经过周期数 = 当前周期 − 该仓位 lastClaimPeriod
  计算收益   = principal × 1% × 经过周期数
  可发放额   = min(计算收益, principal × 3 − earned)      ← 各自封顶
  earned    += 可发放额
  若 earned ≥ principal × 3  → closed = true（该仓位出局）
  lastClaimPeriod = 当前周期
发放总额 = Σ 各仓位可发放额
```

**要点**：
- 每个仓位用自己的 `lastClaimPeriod`，补投时间不同 → 进度天然独立
- 团队奖基于「本次实发静态收益总额」计算，**算法不变**（静态收益算对了，动态奖自动正确）

### 4.3 动态收益（下级投资 / 下级结算时触发）

动态收益进入用户账户时，**按仓位顺序填充**：

```
剩余待分配 = 动态收益金额
遍历仓位（从最早开始）：
    该仓位剩余额度 = principal × 3 − earned
    if 剩余额度 == 0 → 跳过（已满）
    实际计入 = min(剩余待分配, 该仓位剩余额度)
    该仓位 earned += 实际计入
    剩余待分配 -= 实际计入
    if 该仓位 earned 达到上限 → closed = true
所有仓位都已满 → 剩余部分不再发放（3 倍封顶）
```

### 4.4 出局判定

| 层级 | 判定 | 结果 |
|---|---|---|
| 单仓位 | `earned ≥ principal × 3` | 该仓位 `closed = true`，停止产生静态收益，也不再接收动态收益 |
| 账号 | 所有仓位 `closed` | 账号 `exited = true`，`Exited` 事件 |

### 4.5 复投（出局后再次投资）

```
账号已出局后再投资 → 新建一个仓位（本金 = 新投资额）
  ├─ 历史仓位保留（不清空，仅作为历史记录）
  ├─ ⚠️ pendingUSDT / pendingXMR / xmrWithdrawalPending 一律保留（本次一并修复）
  ├─ 账号 exited = false，重新激活
  └─ 新仓位独立开始 3 倍周期
```

---

## 五、⚠️ 关键技术风险：Gas 上限（必须配套解决）

### 5.1 风险

现架构中 `dailySettlement` 需遍历**全部用户**（当前 204 人），每人还要沿推荐链计算团队奖，**已接近 BSC 单笔交易 gas 上限**。引入多仓位后循环次数翻倍（人均 2 个仓位即翻倍），**极易 gas 超限 → 结算交易失败 → 当日收益发不出去**（最严重的事故）。

### 5.2 解决方案：分页结算

```
合约侧：
  - lastSettlementPeriod：本轮结算的周期号
  - settlementCursor：本轮已处理到的用户索引

dailySettlement(uint256 _xmrPrice)：
    currentPeriod = 当前周期
    if (currentPeriod > lastSettlementPeriod) {
        lastSettlementPeriod = currentPeriod
        settlementCursor = 0                    // 新周期，游标归零
        xmrPrice = _xmrPrice                    // 更新价格
    }
    end = min(settlementCursor + BATCH_SIZE, userList.length)   // BATCH_SIZE 可配（如 50）
    for i in [settlementCursor, end):
        _settleUser(userList[i], currentPeriod)
    settlementCursor = end

后端侧：
  每天 12:01 循环调用 dailySettlement，直到 settlementCursor ≥ userList.length
  （每笔交易等待确认后再发下一笔，避免 nonce 冲突）
```

**收益**：单笔交易 gas 可控，永不超限；成本是结算由 1 笔变 N 笔（后端自动完成，运营无感）。

### 5.3 配套优化

- **仓位数量上限**：建议单账号最多 **20 个仓位**；
- **已满仓位清理**：`closed = true` 且超过上限时，可从数组移除（或标记删除），避免数组无限增长导致 gas 上升；
- 团队奖链深度沿用现有 `MAX_TEAM_DEPTH = 50` / `MAX_GENERATIONS = 12` 限制。

---

## 六、前端影响

**前端无需改动**，原因：

| 前端使用的字段 | 数据来源 | 变化 |
|---|---|---|
| `personalAmount` | 合约汇总返回 | 口径不变（求和） |
| `exitLimit` | 合约汇总返回 | 口径不变（求和） |
| `totalEarned` | 合约汇总返回 | 口径不变（求和） |
| `pendingXMR` / `pendingUSDT` | 合约直接记录 | 不变 |
| 出局进度条 | `totalEarned / exitLimit` | 展示逻辑不变 |

唯一变化是**进度推进速度**（正是本次改造目的），用户可感知的是"收益发放节奏"而非界面。

---

## 七、数据迁移方案

### 7.1 迁移映射（现有 204 个用户）

每个用户在新合约生成 **1 个仓位**：

```solidity
仓位[0].principal        = 旧 personalAmount
仓位[0].earned           = 旧 totalEarned
仓位[0].lastClaimPeriod  = 旧 lastClaimDay
仓位[0].closed           = (旧 totalEarned >= 旧 exitLimit)
```

### 7.2 迁移后校验（必须全部通过）

| 校验项 | 要求 |
|---|---|
| 用户数 | 新旧一致 |
| 每用户 personalAmount | 完全一致 |
| 每用户 totalEarned | 完全一致 |
| 每用户 exitLimit | 完全一致 |
| 每用户 pendingXMR / pendingUSDT | 完全一致 |
| 每用户 referrer / level / 团队业绩 | 完全一致 |
| 推荐关系树 | 完全一致 |

> 建议：迁移脚本生成校验报告（逐用户对比），全部通过后才切换前端合约地址。

---

## 八、测试网验证计划

### 8.1 环境

- 部署到 **BSC 测试网**（chainId 97），MockUSDT 公开铸币，无需真实资金
- **结算周期临时改为 30 分钟或 1 小时**（`SETTLEMENT_INTERVAL = 1800 / 3600`），便于快速验证
- 验证通过后改回 24 小时（锚定北京时间 12:00）再部署主网

### 8.2 测试用例清单

| # | 场景 | 预期结果 |
|---|---|---|
| 1 | 单仓位每日收益 | 按本金 1%/天 正确累计 |
| 2 | 补投生成仓位 2 | 两仓位独立进度、互不干扰 |
| 3 | 仓位 1 拿满 | 仓位 1 停止发放，仓位 2 继续 |
| 4 | **动态收益按顺序填充** | 需求实例：+20 补满仓位 1，+80 进仓位 2 |
| 5 | 所有仓位拿满 | 账号出局，`Exited` 事件，停止发放 |
| 6 | 出局后复投 | 新建仓位重新开始；**未提取的 XMR/USDT 保留不清零** |
| 7 | 分页结算 | 多批调用后所有人收益正确、不重复、不遗漏 |
| 8 | 团队奖链 | 直推奖 / 级差奖 / 平级超越奖基于实发金额计算正确 |
| 9 | 边界：收益刚好补齐额度 | 封顶后多余部分不再发放 |
| 10 | 边界：跨多周期未结算 | 按周期数补发，且不超过 30 周期上限 |
| 11 | Gas 实测 | 单批（50 用户 × 平均多仓位）gas 远低于区块上限 |
| 12 | 迁移校验 | 迁移后所有用户数据与旧合约逐项一致 |

### 8.3 通过标准

- 上述 12 项全部通过
- 分页结算连续跑 3 天无失败
- 迁移校验报告 100% 一致

---

## 九、上线流程

```
1. 测试网部署 + 30 分钟周期验证（全部用例通过）
2. 周期改回 24h（北京 12:00 锚定），重新部署测试网复核
3. 主网部署新合约（多签托管所有权）
4. 用户数据迁移 + 校验报告
5. 后端切换合约地址（.env）+ 分页结算逻辑上线
6. 前端切换合约地址 + 重新构建部署
7. 上线后首日盯盘：结算日志、gas 消耗、用户收益抽查
8. 旧合约保留只读（便于核对历史数据），不做进一步操作
```

---

## 十、待确认事项（需业务方拍板）

| # | 事项 | 建议 |
|---|---|---|
| 1 | **多余动态收益**：所有仓位都满后，再来的动态收益是丢弃还是记入待提取余额？ | 按 3 倍出局规则**丢弃**（与现逻辑一致） |
| 2 | **仓位数量上限**：是否限制？（超过后老仓位如何处理） | 上限 **20 个**，已满的老仓位可清理 |
| 3 | **出局后复投**：未提取的 XMR/USDT 是否保留？ | **保留**（本次一并修复） |
| 4 | **补投限制**：是否允许任意时间、任意金额（≥100 且 100 倍数）补投？ | 保持现有规则不变 |
| 5 | **分页批次大小**：每批处理多少用户？ | **50 个/批**，可按 gas 实测调整 |
| 6 | **XMR 提现门槛**：是否从 0.05 降低（减少用户累积等待）？ | 可选，建议维持 0.05 |

---

## 十一、影响面清单（开发时的改动点）

| 文件 | 改动内容 |
|---|---|
| `contracts/StakingDApp.sol` | 新增 Position 结构、改造 invest / _settleUser / _distributeTeamRewards / getUserInfo、新增分页结算 |
| `backend/src/services/settlementScheduler.js` | 分页结算循环调用 |
| `backend/src/services/eventScanner.js` | 新增事件类型（如 PositionClosed）扫描 |
| `backend/src/config/abis.js` | 新 ABI |
| `frontend/src/config/abis.js` | 新 ABI（getUserInfo 结构可能微调） |
| `frontend/src/config/contracts.js` | 新合约地址 |
| `scripts/migration/*` | 用户数据迁移 + 校验脚本 |
| 测试 | `test/StakingDApp.test.js` 新增多仓位用例 |

---

## 十二、风险提示

1. **Gas 风险（最高）**：不做分页结算，多仓位方案上线后极可能在用户量增长时结算失败 → 必须配套实施
2. **迁移风险**：迁移脚本必须先在测试网跑通并输出校验报告，主网迁移前做数据快照备份
3. **用户感知风险**：改造后"出局进度推进速度"变化，可能引起用户咨询 → 建议提前准备客服话术
4. **多签依赖**：新合约部署后所有权移交多签，后续参数调整需 2/3 签名
5. **旧合约历史**：旧合约的收益数据继续保留（用户可查询），但不再产生新收益
