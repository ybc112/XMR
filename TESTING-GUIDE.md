# XMR Staking DApp 测试网操作指南

> 环境：BSC 测试网 ｜ 前端：https://xmr-beta.vercel.app/ ｜ 管理页：https://xmr-beta.vercel.app/admin

---

## 一、环境准备（约 10 分钟）

### 1.1 准备测试钱包

建议准备 **3 个钱包**，角色如下：

| 钱包 | 角色 | 用途 |
|---|---|---|
| 钱包 A | 创世用户 | 第一个注册、投资、领收益 |
| 钱包 B | 一级直推 | 测试推荐奖、团队等级 |
| 钱包 C | 管理员 | 打开管理页（/admin），测试管理功能 |

### 1.2 安装/准备 MetaMask

- 安装 [MetaMask](https://metamask.io/) 并创建 3 个账户
- 打开 https://xmr-beta.vercel.app/ 点「连接钱包」，MetaMask 会**自动提示切换到 BSC 测试网**（chainId 97，自动添加，无需手动配置）

### 1.3 领取测试币

| 币种 | 获取方式 |
|---|---|
| **tBNB**（gas 费） | 官方水龙头：https://www.bnbchain.org/en/testnet-faucet （连接钱包/登录后领取，每个钱包领 0.1~0.5 tBNB） |
| **测试 U**（假 USDT） | 测试网专用假 USDT，公开铸币任何人可领，无需授权。**要在区块链浏览器操作，详细步骤见 1.4** |

### 1.4 区块链浏览器操作：领取测试 U

测试 U 需要在 BscScan 测试网上操作（这是测试流程里唯一的浏览器操作之一，多签的备选操作见流程 8）：

1. 打开测试 U 合约页面：https://testnet.bscscan.com/address/0xBCE748bd6A0B93318f2B0DebA2Efc59f5107072C
2. 点页面右侧 **Connect to Web3**（连接钱包），MetaMask 选择要领取的钱包并确认
3. 切到 **Contract（合约）** 标签 → 点 **Write Contract（写入合约）** 子页签
4. 找到 **mint** 函数，在数量栏填入要铸造的金额（数值见下表），点 **Write** 按钮
5. MetaMask 弹窗点「确认」，等待上链（约 10~30 秒）
6. 回到 DApp「资产」页，余额即到账

| 想要的数量 | 数量栏填写值 |
|---|---|
| 1,000 U | 1000000000000000000000 |
| 10,000 U | 10000000000000000000000000 |
| 100,000 U | 100000000000000000000000000 |

> 建议：钱包 A、B 各领 10 万 U，钱包 C 领 1 千 U。
> 同一个合约页的 **Read Contract（读取合约）** 子页签是查数据的（比如查某个钱包是不是管理员、查余额），后面验证时也会用到。

### 1.5 核对准备完成

首页（仪表盘）能看到：`XMR 价格 100.0`、`日化率 1.00%`、`合约地址为测试网`（浏览器打开地址是 testnet.bscscan.com）即就绪。

---

## 二、核心业务流程测试（按顺序）

### 流程 0：创世注册（钱包 A）—— 必测

| 步骤 | 操作 | 预期 |
|---|---|---|
| 1 | 钱包 A 连接，进入「算力」页 | 显示「注册」标签页 |
| 2 | **推荐人地址留空**，点「立即注册」 | ✅ 注册成功提示（第一个用户） |
| 3 | 页面刷新 | 注册 Tab 消失，出现「投资」Tab |

**错误场景验证：**

| 操作 | 预期报错 |
|---|---|
| 推荐人填自己的地址 | 「不能填写自己的地址作为推荐人」 |
| 推荐人填未注册的随机地址 | 「推荐人尚未注册」 |
| 再点一次注册 | 「该钱包已经注册过了」 |

> 会员 ID 为 **10001~1010000 随机生成**（100 万名额，非顺序递增），注册成功后可在仪表盘/团队页查看。

### 流程 1：投资（钱包 A）—— 必测

| 步骤 | 操作 | 预期 |
|---|---|---|
| 1 | 「投资」Tab，输入 `100` U | 显示「当前日化率 1.00%」 |
| 2 | 点「投资」 | MetaMask 先弹 **USDT 授权**，确认后再弹投资交易 |
| 3 | 确认 | 投资成功，仪表盘出现个人业绩、3 倍出局额度（300 U） |

**边界测试：**

| 输入 | 预期 |
|---|---|
| `99` U | ❌ 低于最低投资金额（100 USDT） |
| `150` U | ❌ 不是 100 的整数倍（投资必须是 100 的倍数） |
| `100` U | ✅ 成功，出局额度 = 300 U |
| `100000` U | ✅ 成功（大额无上限） |

### 流程 2：静态收益（钱包 A）—— 必测

| 步骤 | 操作 | 预期 |
|---|---|---|
| 1 | 投资完成后，回到「算力」页点「领取收益」 | 当天注册当天领 → ❌ 提示「今天已经领取过了」或收益为 0（合约设计：注册当天不可领，次日开始） |
| 2 | **等一天**（测试网 1 天 = 86400 秒，可用 [Block Time Tool](https://clock.casperdapp.com/) 或直接等明天） | 再点领取 ✅ 收益 = 100 × 1% = 1 U（换算成 XMR 记入待兑资产） |
| 3 | 连续 2 次点领取（同一天） | 第二次 ❌ 「今天已经领取过了」 |
| 4 | 连续 5 天不领，一次领取 | ✅ 一次性补领 5 天 = 5 U（最多补 30 天） |
| 5 | **出局后复投当天**点「领取收益」 | ❌ 不可领（v3.1 修复：复投当天与注册当天规则一致，次日才能领；此前版本存在复投当天可白领一天的 bug） |
| 6 | 复投后**等一天**再领 | ✅ 正常领取，收益按复投后的本金计算 |

**验证点**：仪表盘「可领取收益」数字与 XMR 换算一致（XMR 价格 100 U → 1 U = 0.01 XMR）。

### 流程 3：直推与 12 代推荐奖（钱包 B）—— 必测

| 步骤 | 操作 | 预期 |
|---|---|---|
| 1 | 钱包 B 连接，注册时**推荐人填钱包 A** | ✅ 注册成功 |
| 2 | 钱包 B 投资 100 U | 钱包 A 的「待领取 USDT」（团队收益）增加 `100 × 10% = 10 U`（一代推荐奖） |
| 3 | 钱包 A 打开「团队」页 | 直推列表出现钱包 B，网络树正确 |
| 4 | （可选）钱包 B 再注册 C，C 投资 | 钱包 A 拿 2 代奖 `3%`，钱包 B 拿 1 代奖 `10%` |

> 推荐奖比例（前 12 代）：第 1 代 10%，第 2 代 3%，第 3 代 2%，第 4-12 代 1%。
> 注意：上家需「活跃」（自己投资 ≥100 U 且未出局、未拉黑）才能拿奖。

### 流程 4：团队等级（M1-M9）—— 建议测

等级考核 = **个人业绩 + 小区业绩**（团队页进度条按此显示）：

| 等级 | 个人业绩 | 小区业绩 | 团队奖励比例 |
|---|---|---|---|
| M1 | 200 U | 5,000 U | 5% |
| M2 | 500 U | 20,000 U | 10% |
| M3 | 1,000 U | 80,000 U | 15% |
| M4 | 2,000 U | 200,000 U | 20% |
| M5 | 3,000 U | 500,000 U | 25% |
| M6 | 5,000 U | 1,000,000 U | 30% |
| M7 | 10,000 U | 2,000,000 U | 35% |
| M8 | 15,000 U | 5,000,000 U | 40% |
| M9 | 20,000 U | 10,000,000 U | 45% |

**测试建议**：钱包 A 补投到 200 U（个人业绩达标），钱包 B/C 各投 2,500 U（B、C 分属两个直推分支，形成小区业绩），刷新团队页看是否升到 M1。

> 小区业绩 = 团队总业绩 − 最大分支业绩（合约口径）。
>
> **团队奖基数**：伞下账户领取的**静态收益**（非投资入金）。
> 例：D 投 100 U，次日产生 1% = **1 U 价值 XMR** 的静态收益，上级 C(M1,5%) 拿
> 1 U × 5% = **0.05 U 价值 XMR** 的团队奖。团队奖以 **XMR** 记账（pendingXMR）。
>
> **平级奖 / 超越奖**（合约 `_distributeTeamRewards`）：
> 上级拿「紧邻下级」该笔**团队奖励**的 **10%**。
>
> 平级奖示例：A(M1) → B(M1) → 投资人 D 投 1000 U，次日 D 静态收益 10 U 价值
> - B（M1，5%）级差奖 = 10 × 5% = **0.5 U** → A 与 B 平级，平级奖 = 0.5 × 10% = **0.05 U**（价值 XMR）
>
> 超越奖示例：A(M1) → B(M2) → 投资人 D 投 1000 U，次日 D 静态收益 10 U 价值
> - B（M2，10%）级差奖 = 10 × 10% = **1 U** → A 等级低于 B，超越奖 = 1 × 10% = **0.1 U**（价值 XMR）

### 流程 5：闪兑（钱包 A）—— 必测

| 步骤 | 操作 | 预期 |
|---|---|---|
| 1 | 领完静态收益后进入「闪兑」页 | 显示可兑 XMR 数量 |
| 2 | 输入要兑换的 XMR 数量，点「闪兑」 | ✅ 成功，USDT 到账（按当前链上 XMR 价格 100 U 折算），XMR 被销毁 |
| 3 | 输入超过持有量的数量 | ❌ 余额不足报错 |

### 流程 6：USDT 提现（钱包 A）—— 必测

> ⚠️ 提现金额**必须是 10 的整数倍**（如 24 U 只能提 20 U）。

| 步骤 | 操作 | 预期 |
|---|---|---|
| 1 | 「资产」页，输入提现金额（如 50 U） | 显示手续费 5%（2.5 U） |
| 2 | 点「提现」 | ✅ 即时到账，实收 47.5 U（50 − 5%） |
| 3 | 边界：输入 `24` U | ❌ 提示「提现金额必须是 10 的整数倍」；输入 `20` U ✅ |

### 流程 7：XMR 提现申请 + 管理员审核 —— 必测（走通中心化环节）

> ⚠️ v3 起：**必须先绑定门罗 XMR 收款地址**才能申请提现（流程 7 第 0 步）。

| 步骤 | 操作 | 预期 |
|---|---|---|
| 0 | 「资产」页 XMR 卡片 → 添加 XMR 收款地址（门罗地址，95~106 位，`4` 或 `8` 开头） | ✅ 保存成功；未设置地址时「提现 XMR」按钮置灰 |
| 1 | 钱包 A「资产」页 →「申请 XMR 提现」输入 ≥ 0.05 XMR | ✅ 申请成功，5% 手续费直接销毁，余额进入「待审核」 |
| 2 | 钱包 C（管理员）打开管理页（/admin） | 看到待处理 XMR 提现列表（含用户的门罗收款地址） |
| 3 | 钱包 C 点「处理提现」 | ✅ 用户 XMR 到账 |

> 若钱包 C 不是管理员：需要先由多签 owner 走一遍流程 8 添加管理员。

### 流程 7.5：管理后台（管理页）—— 必测

**入口**：浏览器打开 **https://xmr-beta.vercel.app/admin**（或站点内点导航栏「管理」）→ 连接钱包即可进入。

**两种身份的行为**：

| 身份 | 行为 |
|---|---|
| 普通钱包 | ✅ 能进入、能查看所有数据（总用户/总投资/价格/费率/待处理提现/多签列表等）；顶部有黄色提示「当前钱包不是管理员：仅可查看」；点任何操作按钮都会提示「当前钱包不是管理员，无法执行该操作」 |
| 管理员钱包 | ✅ 全部功能可用：参数设置（XMR 价格/日化率/算力/提现费率）、黑名单、紧急暂停/恢复、处理 XMR 提现、多签操作 |

> 成为管理员：需由多签 owner 走一遍流程 8 添加（管理员 = 3 个多签 owner 钱包或已添加的钱包）。

### 流程 8：多签管理（3 个 owner 钱包）—— 首次上线前必测

> 规则：3 个 owner 钱包，**任意 2 个签名即生效**。所有管理操作（设价格、改费率、加管理员、处理提现）都要经过多签。

在 **https://xmr-beta.vercel.app/admin** 页面操作：

| 步骤 | 操作 | 预期 |
|---|---|---|
| 1 | **owner 1** 钱包连接 → 页面下方「多签操作」区，填写要执行的操作（如添加管理员：目标地址填主合约地址、交易数据向技术同事索取），点「提交交易」 | ✅ 交易出现在下方「多签交易列表」，状态「待确认」，确认数 1/2，操作内容列显示可读文字（如 `addAdmin(0x...)`） |
| 2 | **owner 2** 钱包连接（切钱包后刷新页面）→ 多签交易列表 → 对该交易点「确认」 | ✅ 确认数变 2/2，交易状态变「可执行」 |
| 3 | **owner 1 或 owner 2** 点「执行」 | ✅ 交易状态变「已执行」，对应操作生效（如目标钱包成为管理员） |
| 4 | 验证：用刚添加的钱包打开 /admin | 操作按钮不再被拦截，可直接使用 |

> 只提交不确认（1 签）或只确认不执行 → 交易挂起，不生效。列表里每笔交易都显示「操作内容」「我已签名」「确认数」，方便分辨。

**备选方式：在区块链浏览器操作多签**（当页面多签区不便操作时，二选一即可，效果完全一样）

1. 打开多签合约页面：https://testnet.bscscan.com/address/0x8A85061dc4C48a65b7b10E0EF41b1a315b889018
2. 点 **Connect to Web3** 连接 **owner1** 钱包 → **Contract** → **Write Contract**
3. **提交**：找到 `submitTransaction`，三项填好后点 **Write** 确认：
   - 目的地（destination）：主合约地址 `0xd8757c0c9646397e9b71f63C1E1e0Ab8963e2ad4`
   - 值（value）：`0`
   - 数据（data）：粘贴技术同事提供的交易数据
4. **确认**：切换 **owner2** 钱包连接 → 找到 `confirmTransaction`，编号（_txId）填多签列表里那笔交易的编号 → Write 确认
5. **执行**：任一 owner 钱包 → 找到 `executeTransaction`，填同一编号 → Write 确认
6. ✅ 完成，操作生效（回到管理页刷新「多签交易列表」可见该笔已执行）

### ⚠️ 历史教训：多签「签不上名」的真实原因（务必阅读）

之前测试时遇到过「第二个多签钱包一直签不上名」的问题，排查后**不是程序 Bug，是操作失误**，原因如下：

| 问题 | 实际情况 |
|---|---|
| 现象 | 三个钱包要添加为管理员，结果只有第一个钱包成功，第二个钱包"怎么签都不生效" |
| 真实原因 | **三笔提交的操作内容全部是「添加第一个钱包」**——同一个操作被重复提交了 3 次，另外两个钱包的操作**根本没被提交过**，所以自然"没得签" |
| 结果 | 链上确实有 3 笔多签交易，但内容一模一样，第一个钱包被加了 3 次（幂等无害），另两个钱包始终不是管理员 |

**操作要点**：
1. 提交前看列表里的**「操作内容」列**，确认这一笔到底在干什么（例如要添加 3 个管理员，应有 3 笔内容不同的交易：`addAdmin(A)`、`addAdmin(B)`、`addAdmin(C)`）
2. 提交时如果内容重复，页面会提示「已存在相同的待执行交易 #N」——这时应检查是否提交错了，而不是继续确认
3. 三个钱包做三件事，就要提交三笔不同的交易，各自走「提交 → 确认 → 执行」

**验证**：添加完 3 个管理员后，管理页底部「多签交易列表」应有 3 笔内容不同的已执行交易；用任意一个刚添加的钱包打开管理页，操作按钮不再被拦截。

---

## 三、进阶/异常场景（可选）

| 场景 | 操作 | 预期 |
|---|---|---|
| 3 倍出局 | 投资 100 U，累计领到 300 U 收益 | 自动出局，收益停止；再次投资会「清零重来」 |
| 出局后复投 | 出局后点「投资」 | 系统重置个人业绩/收益重新计算；**复投当天不可领取静态收益**（次日开始，v3.1 规则） |
| 拉黑用户 | 管理员打开管理页 → 黑名单管理，填入钱包 B | B 无法投资/领取，但可提现 |
| 紧急暂停 | 管理员打开管理页 → 点「紧急暂停」 | 所有人无法注册/投资/领取，页面报「合约已暂停」 |
| 修改费率 | 管理员打开管理页 → 设置日化率/提现费率 | 仪表盘实时更新，费率上限 100% |
| 修改 XMR 价格 | 管理员打开管理页 → 设置 XMR 价格/每日结算 | 闪兑与收益换算按新价格执行 |

---

## 四、测试数据速查

| 数据 | 值 |
|---|---|
| 合约（测试网 v3.1） | StakingDApp `0xd8757c0c9646397e9b71f63C1E1e0Ab8963e2ad4` ｜ XMRToken `0x7f8600a20B88bb713E04c689f70d92BFE965828E` ｜ MultiSig `0x8A85061dc4C48a65b7b10E0EF41b1a315b889018` ｜ MockUSDT `0xBCE748bd6A0B93318f2B0DebA2Efc59f5107072C` |
| 投资规则 | ≥100 U 且为 100 的整数倍 |
| 日化率 | 1%（100 基点 × 算力 100，可按用户覆盖算力） |
| 出局上限 | 3 倍本金 |
| USDT 提现 | 必须为 10 的整数倍，手续费 5%（即时扣） |
| XMR 提现 | 需先绑定门罗收款地址；≥0.05 XMR；手续费 5%（申请时销毁） |
| XMR 初始价格 | 100 U/XMR（管理员可改） |
| 推荐奖（12 代） | 10% / 3% / 2% / 1%×9 |
| 多签 | 3 owners，需 2 签名 |
| 区块浏览器 | https://testnet.bscscan.com |

---

## 五、常见问题

| 现象 | 原因/处理 |
|---|---|
| 连接钱包提示「请切换到 BSC 链」 | 自动切换失败，手动在 MetaMask 添加测试网（chainId 97）后重试 |
| 投资按钮灰色 | 先完成注册；检查 USDT 授权 |
| 「今天已经领取过了」 | 合约按自然日结算，当天只能领一次；领过 X 天的收益后要等次日 |
| 收益为 0 且无报错 | 注册当天 lastClaimDay 已初始化，需等到次日；或已 3 倍出局 |
| 提示「合约已暂停」 | 管理员开了紧急暂停 |
| 交易失败「missing revert data」 | 前端已翻译为中文提示；多半是金额/状态不满足条件 |
| 余额够但授权失败 | 换测试网 RPC 重试（MetaMask 设置里把测试网 RPC 换成 `https://bsc-testnet.bnbchain.org`） |

---

## 六、回归确认清单（验收用）

- [ ] 3 个钱包都能注册（创世留空 + 带推荐人）
- [ ] 投资 100/100000 U 均成功，出局额度 = 3 倍
- [ ] 静态收益次日可领，同一天重复领取被拒，补领 30 天封顶
- [ ] 推荐奖 10%/3%/2%/1% 到账正确
- [ ] 等级 M1 达成条件（个人 200 + 小区 5000）与团队页一致
- [ ] 闪兑按链上价格折算，XMR 销毁
- [ ] USDT 提现 5% 手续费即时到账
- [ ] XMR 提现申请 → 管理员审核 → 到账 全流程
- [ ] 多签 2/3 签名生效（1 签不生效）
- [ ] 3 倍出局自动停止收益，复投重置
- [ ] 复投当天不可领取静态收益（v3.1 修复验证），次日正常领取
- [ ] 黑名单 / 紧急暂停 / 费率修改实时生效
