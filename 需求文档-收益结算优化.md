# XMR 质押 DApp 收益与结算优化需求文档

> 整理时间：2026-08-16  
> 适用项目：`e:\dapp\质押项目3`  
> 目标：将以下 6 项需求整理为可交付给其他 AI 直接修改的开发任务。

---

## 一、背景与现状

当前合约 `StakingDApp.sol` 的收益逻辑：

- `dailyRate = 100`（基点，10000 = 100%）
- `computingPower = 100`（全局算力参数）
- 有效日化率：`effectiveRate = dailyRate * computingPower / 100 = 100`（基点）
- 静态收益 USDT 价值：`personalAmount * effectiveRate * daysPassed / 10000`
  - 默认即投资额 × 1% / 天
- 折算 XMR：`xmrReward = usdtReward * 1e18 / xmrPrice`

也就是说，**合约层面默认已经按投资额 1% 折算 XMR**。但实际产品和测试过程中出现以下问题，需要统一调整。

---

## 二、需求清单

### 需求 1：静态收益必须严格等于投资额 1% 的 XMR 价值

**问题描述**
用户反馈今天收益显示为 2%，与预期的 1% 不符。系统应保证：

- 投资额 100 USDT → 每天收益 1 USDT 价值的 XMR
- 投资额 10,000 USDT → 每天收益 100 USDT 价值的 XMR
- 投资额 20,000 USDT → 每天收益 200 USDT 价值的 XMR

按 XMR 实时价格折算为 XMR 数量。

**期望行为**
1. 前端所有展示「日化率 / 今日收益 / 可领取收益」的地方，必须与 1% 口径一致。
2. 若当前 `dailyRate` 或 `computingPower` 被设置成了非 100 的值（例如 200 / 100 导致 2%），需要：
   - 要么管理员后台把 `dailyRate` 重置为 `100`、全局 `computingPower` 重置为 `100`；
   - 要么合约/前端层面把展示锁定为 1%，不再受 `dailyRate` 影响。
3. `estimateStaticReward`（前端预估收益）和 `claimStaticReward`（实际结算）必须基于同一公式。

**涉及文件**
- `contracts/StakingDApp.sol`：`claimStaticReward`、`estimateStaticReward`
- `frontend/src/utils/format.js`：`formatDailyRate`
- `frontend/src/components/dashboard/Dashboard.jsx`
- `frontend/src/components/admin/Admin.jsx`
- `admin-panel/src/pages/Settings.jsx`
- `admin-panel/src/pages/Dashboard.jsx`

**修改建议**
- 方案 A（推荐）：把合约中的 `dailyRate` 和 `computingPower` 固定为 100，不再允许通过 `setDailyRate` / `setComputingPower` 修改；前端移除相关输入框，仅作只读展示。
- 方案 B：保留合约参数，但在前端显示和预估时写死 `effectiveRate = 1%`。

---

### 需求 2：团队收益每天自动到账，不需要用户点击「领取静态收益」

**问题描述**
当前团队收益（`TeamReward`）是在用户调用 `claimStaticReward()` 时，由 `_distributeTeamRewards` 内部触发并记入上级 `pendingXMR`。这导致：

- 用户不点击领取，团队收益就不会产生和显示。
- 资金明细里看不到团队奖，直到有人领取。

**期望行为**
每天静态收益结算时，系统同时为所有有静态收益资格的用户自动结算其团队收益（级差/平级/超越规则不变），不需要任何用户主动点击领取。

**涉及文件**
- `contracts/StakingDApp.sol`：`claimStaticReward`、`_distributeTeamRewards`、`dailySettlement`
- `frontend/src/components/dashboard/Dashboard.jsx`：最近收益、可领取 XMR
- `frontend/src/components/records/Records.jsx`：资金明细
- `backend/src/services/eventScanner.js`：扫描 `TeamReward` 事件

**修改建议**
1. 合约新增或改造 `dailySettlement(uint256 _xmrPrice)`，使其：
   - 遍历所有注册用户（或维护一个 `usersList`）；
   - 对每个未出局、投资额 ≥ 100 USDT 的用户：
     - 计算并发放其静态收益到 `pendingXMR`；
     - 调用 `_distributeTeamRewards` 沿推荐链发放团队收益；
     - 发出 `StaticRewardClaimed` 和 `TeamReward` 事件。
2. 原 `claimStaticReward` 可保留（允许用户手动提前领取），但日常收益应由 `dailySettlement` 自动发放。
3. 前端「领取静态收益」按钮保留，但收益进度/可领取金额应反映自动累计后的结果。

---

### 需求 3：每天静态收益结算锚定中午 12 点 XMR 实时价格

**问题描述**
当前 `dailySettlement` 只设置 `xmrPrice` 和 `lastSettlementDay`，并不真正发放收益。需要把结算时间与价格锚定机制补齐。

**期望行为**
1. 每天北京时间 / 合约时间 中午 12:00 执行一次 `dailySettlement`。
2. 结算时使用的 XMR 价格必须是中午 12 点的实时价格（通过 `setXMRPrice` 或 `dailySettlement` 传入）。
3. 结算完成后，所有符合条件的用户自动获得静态收益和团队收益（见需求 2）。

**涉及文件**
- `contracts/StakingDApp.sol`：`dailySettlement`、`DAY_SECONDS`、`lastSettlementDay`
- `backend/src/services/blockchain.js`：调用 `dailySettlement` 的封装
- `admin-panel/src/pages/Settings.jsx`：XMR 价格设置、手动触发结算

**修改建议**
1. 合约 `dailySettlement` 内增加检查：
   - 当前时间戳对应的「天」必须大于 `lastSettlementDay`，且 preferably 当前小时 ≥ 12，防止提前结算。
2. 在 `dailySettlement` 内部完成收益发放（结合需求 2）。
3. 运营后台保留手动触发结算的入口，但增加提示「建议在 12:00 后触发」。

---

### 需求 4：首页「收益进度」模块去掉

**问题描述**
首页 Dashboard 下方的「收益进度」卡片与「算力」页面的收益进度重复，需要移除首页的该模块。

**期望行为**
1. `frontend/src/components/dashboard/Dashboard.jsx` 中移除或隐藏「收益进度」Card。
2. 首页保留：总资产、用户信息、我的团队、最近收益、合约状态（管理员）等模块。
3. 「算力」页面（`frontend/src/components/staking/Staking.jsx`）保留其原有的收益进度模块。

**涉及文件**
- `frontend/src/components/dashboard/Dashboard.jsx`

**修改建议**
直接删除 Dashboard 中 `Card title="收益进度"` 及其内部 ProgressBar、reward-display、领取按钮等内容。注意不要把 `handleClaim` 和相关状态完全删除，如果最近收益卡片或算力页面仍需要复用，可保留函数仅移除该 Card。

---

### 需求 5：后台资金流水「绑定 XMR 地址」事件需显示 XMR 地址

**问题描述**
后台用户详情 → 资金流水里，`XMRAddressSet`（绑定 XMR 地址）事件没有展示地址，导致运营人员无法查看用户绑定的 XMR 地址以手动转币。

**期望行为**
1. `XMRAddressSet` 事件在资金流水里显示用户绑定的 XMR 地址。
2. 地址可复制，最好带「复制」按钮。

**涉及文件**
- `admin-panel/src/pages/UserDetailDrawer.jsx`：EventsTab 的 columns
- `backend/src/services/db.js` / `backend/src/services/cache.js`：确保 `args.xmrAddr` 完整返回

**修改建议**
在 EventsTab 的金额列或新增「详情」列中处理 `XMRAddressSet`：

```jsx
if (r.eventType === 'XMRAddressSet') {
  const addr = args.xmrAddr || args.xmrAddress;
  return <CopyableText text={addr} />;
}
```

也可新增一列「地址/详情」专门展示 XMR 地址、USDT 提现地址等字符串类信息。

---

### 需求 6：测试阶段静态收益改为每 30 分钟分发一次

**问题描述**
为了便于测试，当前每日一次的结算需要缩短为每 30 分钟一次。

**期望行为**
1. 测试期间，系统每 30 分钟自动执行一次收益结算。
2. 结算逻辑与每日结算一致：按投资额 1% 的 XMR 价值发放静态收益和团队收益。
3. 正式上线前需改回每日 12:00 一次，或通过配置开关控制。

**涉及文件**
- `contracts/StakingDApp.sol`：`DAY_SECONDS`、`dailySettlement`、`claimStaticReward`
- `backend/src/services/eventScanner.js` 或新增定时任务

**修改建议**
方案 A（推荐测试期使用）：
- 把合约常量 `DAY_SECONDS` 从 `86400` 改为 `1800`（30 分钟）。
- 同步调整 `lastClaimDay` 的语义为「30 分钟周期编号」，或用 `block.timestamp / 1800`。

方案 B（保留天周期，但用外部定时任务）：
- 保留 `DAY_SECONDS = 86400`；
- 在后端新增一个定时任务（如 `node-cron`），每 30 分钟调用一次 `dailySettlement` 并传入当前 XMR 价格；
- 需要在合约层面允许 `dailySettlement` 在不满一天时被调用，或新增一个测试专用的 `testSettlement` 函数。

> 注意：需求 6 与需求 3 的 12 点锚定冲突，测试期优先每 30 分钟，上线后改回 12 点锚定。

---

## 三、关键合约函数速查

| 函数 | 当前作用 | 需要调整 |
|---|---|---|
| `claimStaticReward()` | 用户手动领取静态收益 + 触发团队奖 | 保留，但团队奖改到 `dailySettlement` |
| `_distributeTeamRewards()` | 沿推荐链发放团队奖 | 调用入口从 `claimStaticReward` 改为 `dailySettlement` |
| `dailySettlement(uint256 _xmrPrice)` | 设置当天价格和 lastSettlementDay | 改为真正发放静态+团队收益 |
| `estimateStaticReward(address)` | 预估可领取收益 | 与结算逻辑保持一致 |
| `setDailyRate / setComputingPower` | 调整全局参数 | 如要锁定 1%，需禁用或固定为 100 |

---

## 四、前端页面调整速查

| 页面 | 调整内容 |
|---|---|
| `Dashboard.jsx` | 移除「收益进度」Card；静态收益展示与 1% 口径对齐 |
| `Staking.jsx` | 保留收益进度；确认算力 = 投资额 |
| `Records.jsx` | 静态收益和团队收益事件均正常显示 |
| `Admin.jsx` | 管理面板中收益/日化率展示与 1% 对齐；禁用或移除调整日化率输入 |
| `Users.jsx`（后台） | 已加备注列；算力列已改为投资额 |
| `UserDetailDrawer.jsx`（后台） | 资金流水 `XMRAddressSet` 显示地址 |
| `Settings.jsx`（后台） | 若锁定 1%，移除「设置算力」「日化率」输入 |

---

## 五、数据与事件说明

- `StaticRewardClaimed(user, usdtValue, xmrAmount)`：静态收益事件
- `TeamReward(receiver, investor, level, amount)`：团队收益事件
- `GenerationReward(receiver, investor, generation, amount)`：推荐奖/代数奖事件
- `XMRAddressSet(user, xmrAddr)`：绑定 XMR 地址事件

> 注意：资金明细/流水记录中必须按「事件归属地址」过滤，避免把 `GenerationReward` / `TeamReward` 里的 `investor` 误显示给收益人。该问题已在 `backend/src/services/cache.js` 中通过 `EVENT_OWNER_FIELD_MAP` 修复，后续修改不要回退。

---

## 六、测试验收标准

1. 投资 10,000 USDT，24 小时后「可领取 XMR」≈ 100 USDT 等值 XMR。
2. 不点击领取，24 小时后资金明细中仍出现「静态收益」和「团队奖」事件。
3. 每 30 分钟结算一次时，30 分钟后收益金额 = 投资额 × 1% × (30/1440)。
4. 首页没有「收益进度」模块，算力页面仍有该模块。
5. 后台用户详情资金流水中，「绑定 XMR 地址」行显示完整 XMR 地址并可复制。
6. 管理员后台不能把日化率调到非 1%（或前端显示锁定为 1%）。

---

## 七、风险提示

1. **合约重部署**：若大幅改动 `dailySettlement` 和团队奖触发逻辑，需要重新部署 `StakingDApp` 合约，并更新前后端 `.env` 中的合约地址。
2. **多签要求**：合约 owner 操作需要通过多签钱包（2/3）执行。
3. **测试网先行**：建议先在 BSC 测试网验证 30 分钟结算和自动团队奖逻辑，再部署主网。
4. **状态兼容性**：已有用户的 `lastClaimDay` / `lastSettlementDay` 如果从天切换为 30 分钟周期，需要评估旧数据兼容性或清空重新计算。
